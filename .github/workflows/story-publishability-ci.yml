name: Story publishability tests

on:
  push:
    branches: [fix/global-story-publishability, fix/nearest-story-repairs, fix/story-review-before-telegram, feat/global-story-quality-gate]
  pull_request:
    paths:
      - 'story_publishability.py'
      - 'story_quality_gate.py'
      - 'story_runtime.py'
      - 'story_visual_state.py'
      - 'ready_story_publish.py'
      - 'guarded_story_publish.py'
      - 'send_approved_story.py'
      - 'tests/test_story_publishability.py'
      - 'tests/test_story_publishability_state_order.py'
      - 'tests/test_nearest_story_repairs.py'
      - 'tests/test_story_review_gate.py'
      - 'tests/test_story_quality_gate.py'
      - 'tests/test_story_quality_gate_integration.py'
      - '.github/workflows/story-publishability-ci.yml'

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt
      - run: >-
          python -m unittest
          tests.test_story_publishability
          tests.test_story_publishability_state_order
          tests.test_nearest_story_repairs
          tests.test_story_review_gate
          tests.test_story_quality_gate
