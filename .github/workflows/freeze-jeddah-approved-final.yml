name: Freeze final approved Jeddah deck

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  freeze:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Historical freeze workflow retained for audit; final delivery uses the combined approved artifact."
