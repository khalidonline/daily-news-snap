# Saved package replay

Manual workflow: publishing-v2-saved-replay.yml. Input: existing autopilot shadow source_slot. Main branch only; shared concurrency and budget. No schedule or publication credentials.

Restores exact source URLs and verifies saved text hashes before paid calls. Reuses claims and selected images; no topic, research or photo discovery. Rejects expired attention and changed evidence. Writer keeps the same card count and photo positions; renderer checks saved photo bytes and rights. Reviewer sees original texts and rendered cards.

Results and intermediate drafts persist separately; replay_review_passed never updates readiness or publishes. Source state stays unchanged. Repeated identical recorded attempts stop. Existing bounded agent formatting recovery and budget reservations apply. Source changes require fresh research.

59 local tests passed including five replay tests for role isolation, stale/changed evidence, card counts and rejection persistence. No paid production replay yet.
